# https://taskfile.dev

version: '3'

vars:
  GREETING: Hello, World!

tasks:
  default:
    cmds:
      - cargo build -release
  arm64:
    cmds: 
      # - rustup target add aarch64-unknown-linux-gnu  # 安装依赖、glibc 环境
      # - cargo install cross
      - cross build --release --target aarch64-unknown-linux-musl
